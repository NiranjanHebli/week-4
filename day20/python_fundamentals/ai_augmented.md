## Prompt :- 
Explain Python dunder methods for beginners with simple examples of __init__, __str__, and __len__. Include one custom class using these methods and briefly show how each method works with output. Keep the explanation short and beginner-friendly.


## AI - Generated Output:-

### [ChatGPT Response](https://chatgpt.com/s/t_69badd54eedc8191963c88320138f905)


### Are the examples correct?
Yes, the examples are correct because they demonstrate the basic purpose of Python dunder methods clearly. `__init__` is used to assign initial values when the object is created, `__str__` provides a readable output when the object is printed, and `__len__` allows the object to work with the len() function.

### Is the class implementation working?
Yes, the class implementation is working correctly. When the Student  object is created, the values are stored properly, print(s1) automatically calls `__str__`, and len(s1) automatically calls `__len__`, producing the expected output.
